apiVersion: v1
kind: Service
metadata:
  name: module10-app-bg
  namespace: apps
spec:
  type: ClusterIP
  selector:
    app: module10-app-bg
    color: blue   # Change this to "green" when switching
  ports:
  - port: 80
    targetPort: 5000
 #   nodePort: 30080